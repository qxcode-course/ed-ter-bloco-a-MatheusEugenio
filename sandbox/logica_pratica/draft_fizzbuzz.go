package main

import "fmt"

func main() {
	var n int
	fmt.Scan(&n)

	for i := 1; i <= n; i++ {
		// TODO: Complete a lógica FizzBuzz
		// Se i % 3 == 0 e i % 5 == 0: "FizzBuzz"
		// Se i % 3 == 0: "Fizz"
		// Se i % 5 == 0: "Buzz"
		// Senão: fmt.Print(i)
		// Separe por espaço, nova linha no final
		fmt.Println("COMPLETE AQUI")
	}
}

